function Geiss__Warp_Of_Dali_2() {
  with (Math) {
    gamma=2;
    echo_zoom=2;
    echo_alpha=0;
    echo_orient=0;
    decay=0.98;
    wave_mode=2;
    wave_additive=1;
    wave_usedots=0;
    bModWaveAlphaByVolume=0;
    wave_brighten=1;
    wrap=1;
    darken_center=0;
    bMotionVectorsOn=0;
    mv_x=12;
    mv_y=9;
    wave_a=2.7;
    fWaveScale=1.053;
    fWaveSmoothing=0.8;
    wave_mystery=0;
    fModWaveAlphaStart=0.75;
    fModWaveAlphaEnd=0.95;
    warp_speed=1;
    warp_scale=2.853;
    zoomexp=1.007318;
    fShader=0;
    zoom=1.046;
    rot=0.02;
    cx=0.5;
    cy=0.5;
    dx=0;
    dy=0;
    warp=0.969;
    sx=1;
    sy=1;
    wave_r=0.6;
    wave_g=0.6;
    wave_b=0.6;
    wave_x=0.5;
    wave_y=0.47;
    wave_r = wave_r + 0.400*( 0.60*sin(0.933*time) + 0.40*sin(1.045*time) );
    wave_g = wave_g + 0.400*( 0.60*sin(0.900*time) + 0.40*sin(0.956*time) );
    wave_b = wave_b + 0.400*( 0.60*sin(0.910*time) + 0.40*sin(0.920*time) );
    zoom = zoom + 0.010*( 0.60*sin(0.339*time) + 0.40*sin(0.276*time) );
    rot = rot + 0.050*( 0.60*sin(0.381*time) + 0.40*sin(0.579*time) );
    cx = cx + 0.030*( 0.60*sin(0.374*time) + 0.40*sin(0.294*time) );
    cy = cy + 0.030*( 0.60*sin(0.393*time) + 0.40*sin(0.223*time) );
    // per_pixel_1=sx=sx-0.04*sin((y*2-1)*6+(x*2-1)*7+time*1.59);
    // per_pixel_2=sy=sy-0.04*sin((x*2-1)*8-(y*2-1)*5+time*1.43);
    // per_pixel_3=zoom=zoom+rad*0.02;
  }
}
register_preset( Geiss__Warp_Of_Dali_2);
