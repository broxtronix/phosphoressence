function Krash__Windowframe_To_Mega_Swirl_2() {
  with (Math) {
    fRating=2.;
    gamma=2.;
    decay=0.98;
    echo_zoom=2.;
    echo_alpha=0.;
    echo_orient=0;
    wave_mode=2;
    wave_additive=0;
    wave_usedots=0;
    bModWaveAlphaByVolume=0;
    wave_brighten=1;
    wrap=0;
    darken_center=0;
    bMotionVectorsOn=0;
    bRedBlueStereo=0;
    mv_x=12;
    mv_y=9;
    brighten=0;
    darken=0;
    solarize=0;
    invert=0;
    wave_a=3.3;
    fWaveScale=1.17;
    fWaveSmoothing=0.5;
    wave_mystery=0.;
    fModWaveAlphaStart=0.75;
    fModWaveAlphaEnd=0.95;
    warp_speed=1.;
    warp_scale=2.853;
    zoomexp=2.1;
    fShader=0.;
    zoom=1.025;
    rot=0.;
    cx=0.5;
    cy=0.5;
    dx=0.;
    dy=0.;
    warp=1.29077;
    sx=1.;
    sy=1.;
    wave_r=0.8;
    wave_g=0.5;
    wave_b=0.3;
    wave_x=0.5;
    wave_y=0.5;
    ob_size=0.01;
    ob_r=0.;
    ob_g=0.;
    ob_b=0.;
    ob_a=1.;
    ib_size=0.01;
    ib_r=0.25;
    ib_g=0.25;
    ib_b=0.25;
    ib_a=0.;
    wave_r = wave_r + 0.400*( 0.60*sin(0.933*time) + 0.40*sin(1.045*time) );
    wave_g = wave_g + 0.400*( 0.60*sin(0.900*time) + 0.40*sin(0.956*time) );
    wave_b = wave_b + 0.400*( 0.60*sin(0.910*time) + 0.40*sin(0.920*time) );
    zoom = zoom + 0.01*( 0.60*sin(0.339*time) + 0.40*sin(0.276*time) );
    rot = rot + 0.010*( 0.60*sin(0.381*time) + 0.40*sin(0.579*time) );
    decay = decay - 0.01*equal(frame%6,0);
    left = 0.5 + 0.25*(sin(0.555*time) + sin(1.111*time));
    right = 0.5 + 0.25*(sin(0.333*time) + sin(1.222*time));
    centrex = (left+right)/2;
    distx = 0.1 + (abs(centrex-left)+abs(centrex-right))/2;
    q1 = centrex + distx;
    q2 = centrex - distx;
    wave_x = centrex;
    top = 0.5 + 0.25*(sin(0.888*time) + sin(0.999*time));
    bottom = 0.5 + 0.25*(sin(0.444*time) + sin(1.333*time));
    centrey = (top+bottom)/2;
    disty = 0.1 + (abs(centrey-top)+abs(centrey-bottom))/2;
    q3 = centrey + disty;
    q4 = centrey - disty;
    wave_y = 1-centrey;
    // per_pixel_1=rot=rot+0.16*sin(time*-3.3+rad*11)*(1.3-rad);
    // per_pixel_2=dx = if_milk(above(x,q1), 1,dx);
    // per_pixel_3=dx = if_milk(below(x,q2), -1,dx);
    // per_pixel_4=dy = if_milk(above(y,q3), 1,dy);
    // per_pixel_5=dy = if_milk(below(y,q4), -1,dy);
  }
}
register_preset( Krash__Windowframe_To_Mega_Swirl_2);
