function Rovastar__Harlequins_Liquid_Dragon() {
  with (Math) {
    fRating=4;
    gamma=1;
    decay=1;
    echo_zoom=0.999609;
    echo_alpha=0;
    echo_orient=0;
    wave_mode=7;
    wave_additive=0;
    wave_usedots=0;
    bModWaveAlphaByVolume=0;
    wave_brighten=1;
    wrap=1;
    darken_center=0;
    bRedBlueStereo=0;
    brighten=0;
    darken=0;
    solarize=0;
    invert=0;
    wave_a=0.001;
    fWaveScale=0.6401;
    fWaveSmoothing=0.27;
    wave_mystery=0;
    fModWaveAlphaStart=0.75;
    fModWaveAlphaEnd=0.95;
    warp_speed=5.99579;
    warp_scale=1.331;
    zoomexp=1.01;
    fShader=0;
    zoom=0.998531;
    rot=0.002;
    cx=0.692;
    cy=0.5;
    dx=0;
    dy=0;
    warp=0.01;
    sx=1;
    sy=1;
    wave_r=0.5;
    wave_g=0.5;
    wave_b=0.5;
    wave_x=0.5;
    wave_y=0.96;
    ob_size=0.01;
    ob_r=0;
    ob_g=0.9;
    ob_b=0.2;
    ob_a=1;
    ib_size=0;
    ib_r=0.5;
    ib_g=0.5;
    ib_b=0.5;
    ib_a=1;
    mv_x=0;
    mv_y=48;
    mv_l=5;
    mv_r=1;
    mv_g=1;
    mv_b=1;
    mv_a=0;
    ob_r = 0.7 - 0.3*(0.5*sin(time*1.701)+ 0.3*cos(time*0.438));
    ob_g = 0.5- 0.4*sin(time*1.724);
    ob_b = 0.5 - 0.35*cos(time*1.196);
    warp =0;
    ib_size = 0.02;
    ib_r = ib_r + 0.5*sin(time*3.034);
    ib_g = ib_g + 0.5*sin(time*2.147);
    ib_b = ib_b - 0.5*sin(time*3.431);
    dx = dx -0.005*sin(time*0.23);
    dy = dy - 0.005*sin(time*0.2);
    volume = 0.15*(bass+bass_att+treb+treb_att+mid+mid_att);
    xamptarg = if_milk(equal(frame%15,0),min(0.5*volume*bass_att,0.5),xamptarg);
    xamp = xamp + 0.5*(xamptarg-xamp);
    xdir = if_milk(above(abs(xpos),xamp),-sign(xpos),if_milk(below(abs(xspeed),0.1),2*above(xpos,0)-1,xdir));
    xaccel = xdir*xamp - xpos - xspeed*0.055*below(abs(xpos),xamp);
    xspeed = xspeed + xdir*xamp - xpos - xspeed*0.055*below(abs(xpos),xamp);
    xpos = xpos + 0.001*xspeed;
    q2 = xpos;
    yamptarg = if_milk(equal(frame%15,0),min(0.3*volume*treb_att,0.5),yamptarg);
    yamp = yamp + 0.5*(yamptarg-yamp);
    ydir = if_milk(above(abs(ypos),yamp),-sign(ypos),if_milk(below(abs(yspeed),0.1),2*above(ypos,0)-1,ydir));
    yaccel = ydir*yamp - ypos - yspeed*0.055*below(abs(ypos),yamp);
    yspeed = yspeed + ydir*yamp - ypos - yspeed*0.055*below(abs(ypos),yamp);
    ypos = ypos + 0.001*yspeed;
    q4 = ypos;
    q5 = max(max(bass,bass_att)-1.2,0);
    // per_pixel_1=box =0.5+0.8*(2*x%2+2*y%2);
    // per_pixel_2=q1 = 2*q5+7.7+0.3*(sin(pow(x,3)+0.137*time)-cos(pow(y,3)+0.213*time));
    // per_pixel_3=zoom = if_milk(above(box,1),q1*.1,zoom);
    // per_pixel_4=rot = if_milk(above(box,1),0.5*sin(0.5*rad+0.385*time),rot);
    // per_pixel_5=cx = cx - 0.5*sin(rad+2*q4);
    // per_pixel_6=cy = cy + 0.11*sin((sqrt(2)-rad)-18*q2);
  }
}
register_preset( Rovastar__Harlequins_Liquid_Dragon);
