function Rovastar_and_Geiss__Dynamic_Swirls_3_Smoking_Delusion_Mix() {
  with (Math) {
    fRating=2;
    gamma=2.7;
    decay=1;
    echo_zoom=2;
    echo_alpha=0;
    echo_orient=0;
    wave_mode=7;
    wave_additive=0;
    wave_usedots=0;
    bWaveThick=0;
    bModWaveAlphaByVolume=0;
    wave_brighten=1;
    wrap=0;
    darken_center=0;
    bRedBlueStereo=0;
    brighten=0;
    darken=0;
    solarize=0;
    invert=1;
    wave_a=1;
    fWaveScale=0.634243;
    fWaveSmoothing=0.1;
    wave_mystery=0;
    fModWaveAlphaStart=0.75;
    fModWaveAlphaEnd=0.95;
    warp_speed=1;
    warp_scale=1.331;
    zoomexp=1;
    fShader=0;
    zoom=1.00496;
    rot=0;
    cx=0.5;
    cy=0.5;
    dx=0;
    dy=0;
    warp=0.000156;
    sx=0.999666;
    sy=0.9999;
    wave_r=0.65;
    wave_g=0.65;
    wave_b=0.65;
    wave_x=0.5;
    wave_y=0.38;
    ob_size=0.01;
    ob_r=1;
    ob_g=0;
    ob_b=0;
    ob_a=1;
    ib_size=0.01;
    ib_r=0.25;
    ib_g=0.25;
    ib_b=0.25;
    ib_a=0;
    mv_x=64;
    mv_y=2.016;
    mv_dx=0;
    mv_dy=-0.1;
    mv_l=5;
    mv_r=0;
    mv_g=0;
    mv_b=0.7;
    mv_a=0;
    wave_r = wave_r + 0.350*( 0.60*sin(0.980*time) + 0.40*sin(1.047*time) );
    wave_g = wave_g + 0.350*( 0.60*sin(0.835*time) + 0.40*sin(1.081*time) );
    wave_b = wave_b + 0.350*( 0.60*sin(0.814*time) + 0.40*sin(1.011*time) );
    tq8 = oldtq8+min(if_milk(above(bass+bass_att,2.5),tq8+0.025*pow((bass+bass_att-2),5),0),1);
    oldtq8 = tq8;
    tq8 = tq8 + time*0.1;
    monitor = tq8;
    q1 = 0.62*( 0.60*sin(0.374*tq8) + 0.40*sin(0.294*tq8) );
    q2 = 0.62*( 0.60*sin(0.393*tq8) + 0.40*sin(0.223*tq8) );
    q3 = 0.62*( 0.60*sin(0.174*-tq8) + 0.40*sin(0.364*tq8) );
    q4 = 0.62*( 0.60*sin(0.234*tq8) + 0.40*sin(0.271*-tq8) );
    q5 = 0.62*( 0.60*sin(0.414*tq8) + 0.40*sin(0.264*tq8) );
    q6 = 0.62*( 0.60*sin(0.234*tq8) + 0.40*sin(0.191*tq8) );
    q7 = 0.62*( 0.60*sin(0.401*tq8) + 0.40*sin(0.307*tq8) );
    q8 = 0.62*( 0.60*sin(0.162*tq8) + 0.40*sin(0.167*-tq8) );
    decay = decay - 0.01*equal(q8%5,0);
    wave_a =0;
    ob_r = wave_r-.2;
    ob_b = wave_b-.2;
    ob_g = wave_g-2;
    ob_r = 0.4 - 0.49*(0.5*sin(time*2.701)+ 0.3*cos(time*0.438));
    ob_g = 0.5- 0.48*sin(time*3.324);
    ob_b = 0.5 - 0.48*cos(time*2.316);
    // per_pixel_1=du = x*2-1 - q1;
    // per_pixel_2=dv = y*2-1 - q2;
    // per_pixel_3=dist = sqrt(du*du+dv*dv);
    // per_pixel_4=ang2 = atan2(du,dv);
    // per_pixel_5=mult = 0.008/(dist+0.4);
    // per_pixel_6=dx = mult*sin(ang2-1);
    // per_pixel_7=dy = mult*cos(ang2-1);
    // per_pixel_8=du = x*2-1 - q3;
    // per_pixel_9=dv = y*2-1 - q4;
    // per_pixel_10=dist = sqrt(du*du+dv*dv);
    // per_pixel_11=ang2 = atan2(du,dv);
    // per_pixel_12=mult = 0.008/(dist+0.4);
    // per_pixel_13=dx = dx + mult*sin(ang2+1);
    // per_pixel_14=dy = dy + mult*cos(ang2+1);
    // per_pixel_15=du = x*2-1 - q5;
    // per_pixel_16=dv = y*2-1 - q6;
    // per_pixel_17=dist = sqrt(du*du+dv*dv);
    // per_pixel_18=ang2 = atan2(du,dv);
    // per_pixel_19=mult = 0.008/(dist+0.2);
    // per_pixel_20=dx = dx + mult*sin(ang2+2);
    // per_pixel_21=dy = dy + mult*cos(ang2+2);
    // per_pixel_22=du = x*2-1 - q7;
    // per_pixel_23=dv = y*2-1 - q8;
    // per_pixel_24=dist = sqrt(du*du+dv*dv);
    // per_pixel_25=ang2 = atan2(du,dv);
    // per_pixel_26=mult = 0.008/(dist+0.2);
    // per_pixel_27=dx = dx + mult*sin(ang2-2);
    // per_pixel_28=dy = dy + mult*cos(ang2-2);
    // per_pixel_29=zoom = 3*dx - 3*dy+1;
    // per_pixel_30=//rot = 3*dx - 3*dy;
    // per_pixel_31=//dx = 0;
    // per_pixel_32=//dy =0;
  }
}
register_preset( Rovastar_and_Geiss__Dynamic_Swirls_3_Smoking_Delusion_Mix);
