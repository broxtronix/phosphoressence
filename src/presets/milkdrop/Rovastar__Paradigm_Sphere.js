function Rovastar__Paradigm_Sphere() {
  with (Math) {
    fRating=2;
    gamma=1.7;
    decay=0.9;
    echo_zoom=1;
    echo_alpha=0;
    echo_orient=3;
    wave_mode=0;
    wave_additive=1;
    wave_usedots=0;
    bWaveThick=1;
    bModWaveAlphaByVolume=0;
    wave_brighten=0;
    wrap=1;
    darken_center=0;
    bRedBlueStereo=0;
    brighten=0;
    darken=0;
    solarize=0;
    invert=0;
    wave_a=4.099998;
    fWaveScale=0.01;
    fWaveSmoothing=0.63;
    wave_mystery=0.018;
    fModWaveAlphaStart=0.71;
    fModWaveAlphaEnd=1.3;
    warp_speed=1;
    warp_scale=1.331;
    zoomexp=1;
    fShader=0;
    zoom=13.290894;
    rot=0;
    cx=0.5;
    cy=0.5;
    dx=-0.28;
    dy=-0.32;
    warp=0.01;
    sx=1;
    sy=1;
    wave_r=0.65;
    wave_g=0.65;
    wave_b=0.65;
    wave_x=0.5;
    wave_y=0.5;
    ob_size=0.015;
    ob_r=0.01;
    ob_g=0;
    ob_b=0;
    ob_a=1;
    ib_size=0.01;
    ib_r=0.95;
    ib_g=0.85;
    ib_b=0.65;
    ib_a=1;
    mv_x=64;
    mv_y=0;
    mv_dx=0;
    mv_dy=0;
    mv_l=0.9;
    mv_r=1;
    mv_g=1;
    mv_b=1;
    mv_a=0;
    wave_r = 0.5 + sin(time/3)/2;
    wave_b = 0.5+ 0.5*sin(time*47);
    wave_g = 0.65 + sin(time*0.23)/10;
    bass_effect = max(0,max(bass_att,bass)-1.2);
    treb_effect = max(0,max(treb_att,treb)-1.3);
    ob_r = 1-wave_r;
    ob_g = 1-wave_g;
    ob_b = 1-wave_b;
    ib_r = 0.75 + 0.25*sin(time*0.4);
    ib_g = 0.25 + 0.25*cos(time*0.87);
    ib_b = 0.5+0.5*sin(1.23*time);
    dx = sin(time*0.895);
    dy = sin(time*1.242);
    q6 = bass_effect;
    volume = 0.15*(bass+bass_att+treb+treb_att+mid+mid_att);
    xamptarg = if_milk(equal(frame%15,0),min(0.5*volume*bass_att,0.5),xamptarg);
    xamp = xamp + 0.5*(xamptarg-xamp);
    xdir = if_milk(above(abs(xpos),xamp),-sign(xpos),if_milk(below(abs(xspeed),0.1),2*above(xpos,0)-1,xdir));
    xaccel = xdir*xamp - xpos - xspeed*0.055*below(abs(xpos),xamp);
    xspeed = xspeed + xdir*xamp - xpos - xspeed*0.055*below(abs(xpos),xamp);
    xpos = xpos + 0.001*xspeed;
    dx = 3*xpos;
    yamptarg = if_milk(equal(frame%15,0),min(0.3*volume*treb_att,0.5),yamptarg);
    yamp = yamp + 0.5*(yamptarg-yamp);
    ydir = if_milk(above(abs(ypos),yamp),-sign(ypos),if_milk(below(abs(yspeed),0.1),2*above(ypos,0)-1,ydir));
    yaccel = ydir*yamp - ypos - yspeed*0.055*below(abs(ypos),yamp);
    yspeed = yspeed + ydir*yamp - ypos - yspeed*0.055*below(abs(ypos),yamp);
    ypos = ypos + 0.001*yspeed;
    dy = 3*ypos;
    // per_pixel_1=myzoom = log(sqrt(2)-rad) -0.05;
    // per_pixel_2=zoom = if_milk(above(rad,0.4),myzoom,0.5+1.1*rad);
  }
}
register_preset( Rovastar__Paradigm_Sphere);
