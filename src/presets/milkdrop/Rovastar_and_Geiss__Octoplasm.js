function Rovastar_and_Geiss__Octoplasm() {
  with (Math) {
    fRating=3;
    gamma=1.7;
    decay=0.964;
    echo_zoom=2;
    echo_alpha=0;
    echo_orient=0;
    wave_mode=2;
    wave_additive=0;
    wave_usedots=0;
    bModWaveAlphaByVolume=0;
    wave_brighten=0;
    wrap=0;
    darken_center=0;
    bRedBlueStereo=0;
    brighten=0;
    darken=0;
    solarize=0;
    invert=0;
    wave_a=1;
    fWaveScale=0.695686;
    fWaveSmoothing=0.45;
    wave_mystery=0;
    fModWaveAlphaStart=0.75;
    fModWaveAlphaEnd=0.95;
    warp_speed=1;
    warp_scale=1;
    zoomexp=1;
    fShader=0;
    zoom=1.02;
    rot=0;
    cx=0.5;
    cy=0.5;
    dx=0;
    dy=0;
    warp=0.076;
    sx=1;
    sy=1;
    wave_r=0.5;
    wave_g=0.5;
    wave_b=0.5;
    wave_x=0.5;
    wave_y=0.5;
    ob_size=0.01;
    ob_r=0.1;
    ob_g=0.2;
    ob_b=0.4;
    ob_a=1;
    ib_size=0;
    ib_r=0.25;
    ib_g=0.25;
    ib_b=0.25;
    ib_a=1;
    mv_x=3.008;
    mv_y=48;
    mv_l=2.9;
    mv_r=1;
    mv_g=1;
    mv_b=1;
    mv_a=0;
    warp = 0;
    bass_effect = min(max(max(bass,bass_att)-1.3,0),1);
    treb_effect = min(max(max(treb,treb_att)-1.3,0),1);
    mid_effect = min(max(max(mid,mid_att)-1.3,0),1);
    wave_r = wave_r + 0.4*( 0.6*sin(1.517*time) + 0.4*mid_effect );
    wave_b = wave_b + 0.4*( 0.6*sin(1.088*time) + 0.4*bass_effect );
    wave_g = wave_g + 0.4*( 0.6*sin(1.037*time) + 0.4*treb_effect );
    rot = rot + 0.040*( 0.60*sin(0.381*time) + 0.40*sin(0.579*time) );
    cx = cx + 0.110*( 0.60*sin(0.374*time) + 0.40*sin(0.294*time) );
    cy = cy + 0.110*( 0.60*sin(0.393*time) + 0.40*sin(0.223*time) );
    ob_r = 1-wave_b;
    ob_g = wave_r;
    ob_b = 1-wave_g;
    q1=cos((0.91*time) + sin(time*0.324));
    q2=time + 0.3*sin((time*0.47) - cos(time*0.216));
    // per_pixel_1=rot=rot+0.08*sin(rad*23.5 + q2*1.3 + q1*1.31);
    // per_pixel_2=zoom=zoom+0.05*sin(ang*4 + pow((sqrt(2)-rad),13*rad) + q2*1.63 + q1) - 0.05;
  }
}
register_preset( Rovastar_and_Geiss__Octoplasm);
