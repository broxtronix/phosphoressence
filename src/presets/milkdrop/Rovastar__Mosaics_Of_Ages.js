function Rovastar__Mosaics_Of_Ages() {
  with (Math) {
    fRating=3;
    gamma=1;
    decay=1;
    echo_zoom=0.999609;
    echo_alpha=0.5;
    echo_orient=1;
    wave_mode=7;
    wave_additive=0;
    wave_usedots=0;
    bWaveThick=0;
    bModWaveAlphaByVolume=0;
    wave_brighten=1;
    wrap=1;
    darken_center=0;
    bRedBlueStereo=0;
    brighten=0;
    darken=0;
    solarize=0;
    invert=0;
    wave_a=0.001;
    fWaveScale=0.6401;
    fWaveSmoothing=0.27;
    wave_mystery=0;
    fModWaveAlphaStart=0.75;
    fModWaveAlphaEnd=0.95;
    warp_speed=5.99579;
    warp_scale=1.331;
    zoomexp=1.01;
    fShader=0;
    zoom=0.998531;
    rot=0.002;
    cx=0.5;
    cy=0.5;
    dx=0;
    dy=0;
    warp=0.01;
    sx=1;
    sy=1;
    wave_r=0.5;
    wave_g=0.5;
    wave_b=0.5;
    wave_x=0.5;
    wave_y=0.96;
    ob_size=0.01;
    ob_r=0;
    ob_g=0.9;
    ob_b=0.2;
    ob_a=1;
    ib_size=0;
    ib_r=0.5;
    ib_g=0.5;
    ib_b=0.5;
    ib_a=1;
    mv_x=9.599999;
    mv_y=9;
    mv_dx=-0.22;
    mv_dy=0;
    mv_l=0.05;
    mv_r=0;
    mv_g=0;
    mv_b=0;
    mv_a=0;
    ob_r = 0.3 - 0.3*(0.5*sin(time*0.701)+ 0.3*cos(time*0.438));
    ob_g = 0.6- 0.4*sin(time*2.924);
    ob_b = 0.35 - 0.3*cos(time*0.816);
    warp =0;
    ib_size = 0.02;
    ib_r = ib_r + 0.5*sin(time*3.034);
    ib_g = ib_g + 0.5*sin(time*2.547);
    ib_b = ib_b - 0.5*sin(time*1.431);
    dx = dx -0.008*sin(time*0.23);
    dy = dy - 0.008*sin(time*0.2);
    
    volume = 0.15*(bass_att+bass+mid+mid_att);
    beatrate = if_milk(equal(beatrate,0),1,if_milk(below(volume,0.01),1,beatrate));
    lastbeat = if_milk(equal(lastbeat,0),time,lastbeat);
    meanbass_att = 0.1*(meanbass_att*9 + bass_att);
    peakbass_att = if_milk(above(bass_att,peakbass_att),bass_att,peakbass_att);
    beat = if_milk(above(volume,0.8),if_milk(below(peakbass_att - bass_att, 0.05*peakbass_att),if_milk(above(time - lastbeat,0.1+0.5*(beatrate-0.1)),1,0),0),0);
    beatrate = max(if_milk(beat,if_milk(below(time-lastbeat,2*beatrate),0.1*(beatrate*9 + time - lastbeat),beatrate),beatrate),0.1);
    peakbass_att = if_milk(equal(beat,0),if_milk(above(time - lastbeat,2*beatrate),peakbass_att*0.95,peakbass_att*0.995),bass_att);
    lastbeat = if_milk(beat,time,lastbeat);
    beatcounter = if_milk(above(beat,0),beatcounter +1, beatcounter);
    beatcounter = if_milk(above(beatcounter,7), 0, beatcounter);
    beateven = beatcounter%2;
    //dx = dx + beateven*0.01;
    q2 = beateven;
    // per_pixel_1=qx = (0&(x*10-0.5))*0.1+0.05;
    // per_pixel_2=qy = (0&(y*10-0.5))*0.1+0.05;
    // per_pixel_3=box=(1-rad)+0.5*abs(x*3-0.4*sin(q1))%2 + 0.5*abs(y*3+0.4*sin(q1))%2;
    // per_pixel_4=box = 3*qy+3*qx;
    // per_pixel_5=q1 = 8.05+(sin(x+0.137*time)-cos(y+0.213*time));
    // per_pixel_6=zoom = if_milk(above(box,1.5),q1*.1,zoom);
    // per_pixel_7=rot = if_milk(above(box,1.5),1*sin(0.385*time),rot);
    // per_pixel_8=cx= q2*(0&(x*4-0.5))*0.25+0.05;
    // per_pixel_9=cy= q2*(0&(y*4-0.5))*0.25+0.05;
  }
}
register_preset( Rovastar__Mosaics_Of_Ages);
