function Rovastar_and_Geiss__Dynamic_Swirls_3_Smoke_Mix() {
  with (Math) {
    fRating=2;
    gamma=2.98;
    decay=1;
    echo_zoom=2;
    echo_alpha=0;
    echo_orient=0;
    wave_mode=7;
    wave_additive=0;
    wave_usedots=1;
    bWaveThick=0;
    bModWaveAlphaByVolume=0;
    wave_brighten=0;
    wrap=0;
    darken_center=0;
    bRedBlueStereo=0;
    brighten=0;
    darken=0;
    solarize=0;
    invert=1;
    wave_a=1.169958;
    fWaveScale=0.634243;
    fWaveSmoothing=0.1;
    wave_mystery=0;
    fModWaveAlphaStart=0.75;
    fModWaveAlphaEnd=0.95;
    warp_speed=1;
    warp_scale=1.331;
    zoomexp=1;
    fShader=0;
    zoom=0.999972;
    rot=0;
    cx=0.5;
    cy=0.5;
    dx=0;
    dy=0;
    warp=0.000156;
    sx=0.999666;
    sy=0.9999;
    wave_r=0.35;
    wave_g=0.35;
    wave_b=0.35;
    wave_x=0.5;
    wave_y=0.38;
    ob_size=0.01;
    ob_r=0;
    ob_g=1;
    ob_b=0;
    ob_a=1;
    ib_size=0.01;
    ib_r=0.25;
    ib_g=0.25;
    ib_b=0.25;
    ib_a=0.1;
    mv_x=64;
    mv_y=2.4;
    mv_dx=0;
    mv_dy=-0.1;
    mv_l=5;
    mv_r=1;
    mv_g=1;
    mv_b=1;
    mv_a=0;
    wave_r = wave_r + 0.350*( 0.60*sin(0.980*time) + 0.40*sin(1.047*time) );
    wave_g = wave_g + 0.350*( 0.60*sin(0.835*time) + 0.40*sin(1.081*time) );
    wave_b = wave_b + 0.350*( 0.60*sin(0.814*time) + 0.40*sin(1.011*time) );
    volume = 0.15*(bass_att+bass+mid+mid_att);
    beatrate = if_milk(equal(beatrate,0),1,if_milk(below(volume,0.01),1,beatrate));
    lastbeat = if_milk(equal(lastbeat,0),time,lastbeat);
    meanbass_att = 0.1*(meanbass_att*9 + bass_att);
    peakbass_att = if_milk(above(bass_att,peakbass_att),bass_att,peakbass_att);
    beat = if_milk(above(volume,0.8),if_milk(below(peakbass_att - bass_att, 0.05*peakbass_att),if_milk(above(time - lastbeat,0.1+0.5*(beatrate-0.1)),1,0),0),0);
    beatrate = max(if_milk(beat,if_milk(below(time-lastbeat,2*beatrate),0.1*(beatrate*9 + time - lastbeat),beatrate),beatrate),0.1);
    peakbass_att = if_milk(equal(beat,0),if_milk(above(time - lastbeat,2*beatrate),peakbass_att*0.95,peakbass_att*0.995),bass_att);
    lastbeat = if_milk(beat,time,lastbeat);
    countertime = if_milk(beat,time,countertime);
    counter =-pow(min((time-countertime-1.5),0),9);
    q7 = min(time-countertime,1);
    q5=oldq5+0.04*counter;
    oldq5=q5;
    q6 = beat;
    //mv_a = q6;
    q1 = 0.62*( 0.60*sin(0.374*q5) + 0.40*sin(0.294*q5) );
    q2 = 0.62*( 0.60*sin(0.393*q5) + 0.40*sin(0.223*q5) );
    q3 = 0.62*( 0.60*sin(0.174*-q5) + 0.40*sin(0.364*q5) );
    q4 = 0.62*( 0.60*sin(0.234*q5) + 0.40*sin(0.271*-q5) );
    ob_r = 0.4 - 0.3*(0.5*sin(time*0.701)+ 0.3*cos(time*0.438));
    ob_g = 0.5- 0.48*sin(time*3.324);
    ob_b = 0.5 - 0.48*cos(time*2.316);
    ib_r = 0.4 - 0.3*(0.5*sin(time*0.801)+ 0.3*cos(time*0.338));
    ib_g = 0.5- 0.48*sin(time*4.8124);
    ib_b = 0.5 - 0.48*cos(time*3.316);
    wave_a = 0;
    // per_pixel_1=du = x*2-1 - q1;
    // per_pixel_2=dv = y*2-1 - q2;
    // per_pixel_3=dist = sqrt(du*du+dv*dv);
    // per_pixel_4=ang2 = atan2(du,dv);
    // per_pixel_5=mult = 0.008/(dist+0.4);
    // per_pixel_6=dx = mult*sin(ang2-1.5);
    // per_pixel_7=dy = mult*cos(ang2-1.5);
    // per_pixel_8=du = x*2-1 - q3;
    // per_pixel_9=dv = y*2-1 - q4;
    // per_pixel_10=dist = sqrt(du*du+dv*dv);
    // per_pixel_11=ang2 = atan2(du,dv);
    // per_pixel_12=mult = 0.015/(dist+0.4);
    // per_pixel_13=dx = dx + mult*sin(ang2+1.5);
    // per_pixel_14=dy = dy + mult*cos(ang2+1.5);
  }
}
register_preset( Rovastar_and_Geiss__Dynamic_Swirls_3_Smoke_Mix);
