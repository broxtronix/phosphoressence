function EMPR__Random__Theyre_so_cute,_Dad_can_I_keep_one() {
  with (Math) {
    fRating=2;
    gamma=2;
    decay=0.94;
    echo_zoom=2;
    echo_alpha=0;
    echo_orient=0;
    wave_mode=4;
    wave_additive=0;
    wave_usedots=0;
    bWaveThick=0;
    bModWaveAlphaByVolume=0;
    wave_brighten=0;
    wrap=1;
    darken_center=0;
    bRedBlueStereo=0;
    brighten=0;
    darken=0;
    solarize=0;
    invert=0;
    wave_a=1;
    fWaveScale=2.651498;
    fWaveSmoothing=0;
    wave_mystery=-0.180163;
    fModWaveAlphaStart=0.75;
    fModWaveAlphaEnd=0.95;
    warp_speed=1;
    warp_scale=1;
    zoomexp=1;
    fShader=0;
    zoom=1;
    rot=0;
    cx=0.5;
    cy=0.5;
    dx=0;
    dy=0;
    warp=0.999997;
    sx=0.578528;
    sy=1.030301;
    wave_r=0.610115;
    wave_g=0.337971;
    wave_b=0.298628;
    wave_x=0.671479;
    wave_y=0.725962;
    ob_size=0.03;
    ob_r=0.148379;
    ob_g=0.786533;
    ob_b=0.637879;
    ob_a=0.973918;
    ib_size=0.045;
    ib_r=0.841289;
    ib_g=0.297099;
    ib_b=0.775799;
    ib_a=0.238807;
    mv_x=12;
    mv_y=9;
    mv_dx=0.59891;
    mv_dy=0.978744;
    mv_l=0.5;
    mv_r=0.296168;
    mv_g=0.490016;
    mv_b=0.461879;
    mv_a=0.573571;
    wave_r = wave_r + 0.45*( 0.6*sin(1.517*time) + 0.4*mid_effect );
    wave_b = wave_b + 0.45*( 0.6*sin(1.088*time) + 0.4*bass_effect );
    wave_g = wave_g + 0.45*( 0.6*sin(1.037*time) + 0.4*treb_effect );
    zoom = zoom + 0.013*( 0.60*sin(0.339*time) + 0.40*sin(0.276*time) );
    rot = rot + 0.020*( 0.60*sin(0.381*time) + 0.40*sin(0.579*time) );
    decay = decay - 0.01*below(frame%5,1);
    volume = 0.3*(bass+mid+att);
    beatrate = equal(beatrate,0) + (1-equal(beatrate,0))*(below(volume,0.01) + (1-below(volume,0.01))*beatrate);
    lastbeat = lastbeat + equal(lastbeat,0)*time;
    meanbass_att = 0.1*(meanbass_att*9 + bass_att);
    peakbass_att = max(bass_att,peakbass_att);
    beat = above(volume,0.8)*below(peakbass_att - bass_att, 0.05*peakbass_att)*above(time - lastbeat, 0.1 + 0.5*(beatrate - 0.1));
    beatrate = max(if_milk(beat,if_milk(below(time-lastbeat,2*beatrate),0.1*(beatrate*9 + time - lastbeat),beatrate),beatrate),0.1);
    peakbass_att = beat*bass_att + (1-beat)*peakbass_att*(above(time - lastbeat, 2*beatrate)*0.95 + (1-above(time - lastbeat, 2*beatrate))*0.995);
    lastbeat = beat*time + (1-beat)*lastbeat;
    peakbass_att = max(peakbass_att,1.1*meanbass_att);
    effect = if_milk(beat,effect+random_integer(5)+1,effect);
    effect = if_milk(above(effect,5),effect-6,effect);
    q1 = effect+2;
    monitor = q1;
    wave_y = treb + wave_b;
    wave_y=if_milk(below(wave_y,0.0), 0.0, wave_y);
    wave_y=if_milk(above(wave_y,1.0), 1.0, wave_y);
    wave_y=if_milk(below(wave_y,0.0), 0.0, wave_y);
    wave_y=if_milk(above(wave_y,1.0), 1.0, wave_y);
    // per_pixel_1=zoom=zoom+0.04*sin(ang*floor(q1)+time*2.6);
    // per_pixel_2=cy = q1 * warp;
    // per_pixel_3=cy = cy * rad;
    // per_pixel_4=cx = a * bass_att;
    // per_pixel_5=dy = warp + time;
    // per_pixel_6=zoomexp = bass_att + a;
    // per_pixel_7=rot = a * bass_att;
    // per_pixel_8=rot = rot * y;
    // per_pixel_9=zoomexp=if_milk(below(zoomexp,0.01), 0.01, zoomexp);
    // per_pixel_10=zoomexp=if_milk(above(zoomexp,100.0), 100.0, zoomexp);
    // per_pixel_11=rot=if_milk(below(rot,-1.0), -1.0, rot);
    // per_pixel_12=rot=if_milk(above(rot,1.0), 1.0, rot);
    // per_pixel_13=cx=if_milk(below(cx,-1.0), -1.0, cx);
    // per_pixel_14=cx=if_milk(above(cx,2.0), 2.0, cx);
    // per_pixel_15=cy=if_milk(below(cy,-1.0), -1.0, cy);
    // per_pixel_16=cy=if_milk(above(cy,2.0), 2.0, cy);
    // per_pixel_17=dy=if_milk(below(dy,-1), -1, dy);
    // per_pixel_18=dy=if_milk(above(dy,1.0), 1.0, dy);
  }
}
register_preset( EMPR__Random__Theyre_so_cute,_Dad_can_I_keep_one);
