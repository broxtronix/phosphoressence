function Rovastar__Intense_Desire() {
  with (Math) {
    fRating=2;
    gamma=1;
    decay=1;
    echo_zoom=1;
    echo_alpha=0;
    echo_orient=3;
    wave_mode=7;
    wave_additive=0;
    wave_usedots=0;
    bWaveThick=1;
    bModWaveAlphaByVolume=0;
    wave_brighten=0;
    wrap=1;
    darken_center=0;
    bRedBlueStereo=0;
    brighten=0;
    darken=1;
    solarize=0;
    invert=0;
    wave_a=1;
    fWaveScale=0.325447;
    fWaveSmoothing=0.5;
    wave_mystery=0;
    fModWaveAlphaStart=0.5;
    fModWaveAlphaEnd=1;
    warp_speed=1;
    warp_scale=1;
    zoomexp=0.999996;
    fShader=0;
    zoom=1;
    rot=0;
    cx=0.5;
    cy=0.5;
    dx=0.00001;
    dy=0.00001;
    warp=0.01;
    sx=1;
    sy=1;
    wave_r=1;
    wave_g=1;
    wave_b=1;
    wave_x=0.5;
    wave_y=0.577;
    ob_size=0.0095;
    ob_r=0;
    ob_g=0;
    ob_b=0;
    ob_a=1;
    ib_size=0.015;
    ib_r=1;
    ib_g=0.6;
    ib_b=0.5;
    ib_a=1;
    mv_x=64;
    mv_y=2.4;
    mv_dx=0;
    mv_dy=-0.1;
    mv_l=5;
    mv_r=1;
    mv_g=1;
    mv_b=1;
    mv_a=1;
    warp = 0;
    ib_r = 0.6 + 0.4*sin(time*0.894);
    ib_g = 0.43 + 0.13*sin(time*7.143);
    ib_b = 0.5+ 0.33*sin(time*0.465);
    thresh = above(bass_att,thresh)*2+(1-above(bass_att,thresh))*((thresh-1.3)*0.96+1.3);
    dx_r = equal(thresh,2)*0.004*sin(5*time)+(1-equal(thresh,2))*dx_r;
    dy_r = equal(thresh,2)*0.004*sin(6*time)+(1-equal(thresh,2))*dy_r;
    cx = dx_r*10 + 0.5;
    cy = dy_r*10 + 0.5;
    q4=cy;
    mv_l =40;
    mv_y =2.0;
    mv_dy = -0.1;
    mv_r =0.7+0.148*(ib_r+ib_b);
    mv_b =1-0.2*(ib_r+ib_b);
    mv_g =0.6+ 0.19*(ib_g+ib_r);
    zoom = zoom -0.01*thresh;
    wave_r = 1-ib_r;
    wave_b = 1-ib_b;
    wave_g = 1-ib_g;
    ob_r = 1-ib_g;
    ob_b = 0.5*(ib_r+ib_g);
    vol = 0.167*(bass+mid);
    xamptarg = if_milk(equal(frame%15,0),min(0.5*vol*bass_att,0.5),xamptarg);
    xamp = xamp + 0.5*(xamptarg-xamp);
    xdir = if_milk(above(abs(xpos),xamp),-sign(xpos),if_milk(below(abs(xspeed),0.1),2*above(xpos,0)-1,xdir));
    xspeed = xspeed + xdir*xamp - xpos - xspeed*0.055*below(abs(xpos),xamp);
    xpos = xpos + 0.001*xspeed;
    dx = 0.025*xpos;
    yamptarg = if_milk(equal(frame%15,0),min(0.3*vol*treb_att,0.5),yamptarg);
    yamp = yamp + 0.5*(yamptarg-yamp);
    ydir = if_milk(above(abs(ypos),yamp),-sign(ypos),if_milk(below(abs(yspeed),0.1),2*above(ypos,0)-1,ydir));
    yspeed = yspeed + ydir*yamp - ypos - yspeed*0.055*below(abs(ypos),yamp);
    ypos = ypos + 0.001*yspeed;
    dy = 0.025*ypos;
    q1= 0.5*(ypos+xpos);
    q2 = ypos;
    // per_pixel_1=rot =0.6* q1*(1-rad);
    // per_pixel_2=rot =rot+ 0.1* q2*(1-rad);
    // per_pixel_3=du = x*2-1 - 0.7;
    // per_pixel_4=dv = y*2-1 + 0.5;
    // per_pixel_5=dist = sqrt(du*du+dv*dv);
    // per_pixel_6=ang2 = atan2(du,dv);
    // per_pixel_7=mult = 0.008/(dist+0.4);
    // per_pixel_8=dx = mult*sin(ang2-1.5);
    // per_pixel_9=dy = mult*cos(ang2-1.5);
    // per_pixel_10=du = x*2-1 - 0.7;
    // per_pixel_11=dv = y*2-1 - 0.5;
    // per_pixel_12=dist = sqrt(du*du+dv*dv);
    // per_pixel_13=ang2 = atan2(du,dv);
    // per_pixel_14=mult = 0.008/(dist+0.4);
    // per_pixel_15=dx = dx + mult*sin(ang2+1.5);
    // per_pixel_16=dy = dy + mult*cos(ang2+1.4);
    // per_pixel_17=dy = dy - if_milk(below(y, 0.65), if_milk(above(y, 0.35), 0.1*(x*y-0.5) + 0.5*(q4-0.4), 0), 0);
    // per_pixel_18=dx = dx - if_milk(above(dx,-0.01), if_milk(below(dx,0), 0.006, 0), 0);
  }
}
register_preset( Rovastar__Intense_Desire);
