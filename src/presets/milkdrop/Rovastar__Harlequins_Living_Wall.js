function Rovastar__Harlequins_Living_Wall() {
  with (Math) {
    fRating=2;
    gamma=1;
    decay=1;
    echo_zoom=0.999609;
    echo_alpha=0;
    echo_orient=0;
    wave_mode=0;
    wave_additive=0;
    wave_usedots=0;
    bWaveThick=0;
    bModWaveAlphaByVolume=0;
    wave_brighten=1;
    wrap=1;
    darken_center=0;
    bRedBlueStereo=0;
    brighten=0;
    darken=0;
    solarize=0;
    invert=0;
    wave_a=28.24947;
    fWaveScale=0.01;
    fWaveSmoothing=0.27;
    wave_mystery=0.05;
    fModWaveAlphaStart=0.75;
    fModWaveAlphaEnd=0.95;
    warp_speed=5.99579;
    warp_scale=1.331;
    zoomexp=1.01;
    fShader=0;
    zoom=0.998531;
    rot=0;
    cx=0.5;
    cy=0.5;
    dx=0;
    dy=0;
    warp=0.01;
    sx=1;
    sy=1;
    wave_r=0.5;
    wave_g=0.5;
    wave_b=0.5;
    wave_x=0.5;
    wave_y=0.5;
    ob_size=0;
    ob_r=0;
    ob_g=0.9;
    ob_b=0.2;
    ob_a=1;
    ib_size=0.025;
    ib_r=0.2;
    ib_g=0.2;
    ib_b=0.2;
    ib_a=1;
    mv_x=1.024;
    mv_y=48;
    mv_dx=0;
    mv_dy=0;
    mv_l=5;
    mv_r=0;
    mv_g=0;
    mv_b=0;
    mv_a=1;
    ob_r = 0.7 - 0.3*(0.5*sin(time*0.701)+ 0.3*cos(time*0.438));
    ob_g = 0.5- 0.4*sin(time*1.324);
    warp =0;
    ib_r = ib_r + 0.2*sin(time*1.034);
    ib_g = ib_g + 0.2*sin(time*1.147);
    ib_b = ib_b - 0.2*sin(time*1.231);
    wave_r = 0.5 + 0.45*(0.5*sin(time*0.701)+ 0.3*cos(time*0.438));
    wave_b = 0.5 -0.4*(0.5*sin(time*4.782)+0.5*cos(time*0.522));
    wave_g = 0.5+0.4*sin(time*1.731);
    volume = 0.15*(bass_att+bass+mid+mid_att);
    beatrate = if_milk(equal(beatrate,0),1,if_milk(below(volume,0.01),1,beatrate));
    lastbeat = if_milk(equal(lastbeat,0),time,lastbeat);
    meanbass_att = 0.1*(meanbass_att*9 + bass_att);
    peakbass_att = if_milk(above(bass_att,peakbass_att),bass_att,peakbass_att);
    beat = if_milk(above(volume,0.8),if_milk(below(peakbass_att - bass_att, 0.05*peakbass_att),if_milk(above(time - lastbeat,0.1+0.5*(beatrate-0.1)),1,0),0),0);
    beatrate = max(if_milk(beat,if_milk(below(time-lastbeat,2*beatrate),0.1*(beatrate*9 + time - lastbeat),beatrate),beatrate),0.1);
    peakbass_att = if_milk(equal(beat,0),if_milk(above(time - lastbeat,2*beatrate),peakbass_att*0.95,peakbass_att*0.995),bass_att);
    lastbeat = if_milk(beat,time,lastbeat);
    countertime = if_milk(beat,time,countertime);
    counter =-1*pow(min((time-countertime-3.7),0),3);
    q3=oldq3+0.002*counter;
    oldq3 =q3;
    monitor = q3;
    cx = cx - 0.4*sin(q3*0.542);
    cy = cy + 0.4*sin(q3*0.753);
    wave_b = 0.45 - 0.3*cos(q3*3.916);
    mv_dx = sin(0.4*q3)*0.125;
    mv_dy =-cos(0.4*q3)*0.1;
    mv_l = (12*sin(q3*1.63));
    monitor = mv_x;
    mv_x = 1.25;
    mv_y =1.25;
    // per_pixel_1=circle = 5*rad*rad*rad;
    // per_pixel_2=q1 = 1.3+0.1*rad+0.5*(sin(0.3*pow(x,4)+x+0.3*q3+3*x*dx - 4*x*dy )-cos(0.2*pow(y,4)+y+q3+ 2*y*rot - 2*y*(1-sy) + (1-sx)));
    // per_pixel_3=zoom = if_milk(above(circle,1),q1*.1,1-0.4*log(sqrt(2)-rad));
    // per_pixel_4=dx = if_milk(above(circle,1),1.3*sin(rot+0.75*x+0.942*q3),0);
    // per_pixel_5=dy= if_milk(above(circle,1),1.3*sin(cx+0.75*y+1.081*q3),0);
    // per_pixel_6=rot = if_milk(above(circle,1),0.4*sin(dx+rad+(x*y)+0.812*q3),0.01*rad);
    // per_pixel_7=sy = if_milk(above(circle,1),1+0.03*sin(dy-rad+(x+y)+1.21*q3),1);
    // per_pixel_8=sx = if_milk(above(circle,1),1+0.03*sin(cy+rad+(x-y)+1.33*q3),1);
  }
}
register_preset( Rovastar__Harlequins_Living_Wall);
